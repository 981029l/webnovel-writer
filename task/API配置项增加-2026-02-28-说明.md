# 功能说明：增加 API Base URL 配置项

## 修改内容
在项目管理页面（`ProjectView.vue`）的"AI 服务配置"选项（`API Base URL`的预设下拉列表）中增加了一个新的预设值：`http://127.0.0.1:8317`。

## 影响范围
- `frontend/src/views/ProjectView.vue` 中的 `apiBaseUrls` 数据数组。

## 完成状态
- [x] 代码修改已完成，本地实时热更新已生效。
